[build]
  command = "npm run build"
  publish = "dist"

# Cache headers - short cache so updates work
[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=3600"
    Access-Control-Allow-Origin = "*"
